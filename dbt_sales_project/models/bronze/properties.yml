version: 2

models:
  - name: bronze_products
    columns:
      - name: category
        tests:
          - accepted_values:
              values: ["Smartwatch", "Mobile", "LED", "Laptop"]
      - name: id
        tests:
          - relationships:
              field: product_id
              to: ref('bronze_orders')

